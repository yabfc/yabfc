dfferent input types:
1. produce x -> done
2. produce max [x,y,z] with available items [a,b,c]
	- also mix? so x and y given, max z with available items
3. produce [x,y,z] - extend 1

input limitations:
1. enabling/disabling machine features
2. limiting effectmodules for a feature


weight configs:
1. energy efficient => weights.power = 1000
2. space efficient => weights.building = 1000
3. ?????
-> need more things that go into cost function
   ressource cost based on rarity?
- also need to balance cost factors


